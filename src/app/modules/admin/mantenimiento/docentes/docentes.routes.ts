import { Routes } from '@angular/router';
import { DocentesComponent } from './docentes.component';

const docentesRoutes: Routes = [
    {
        path: '',
        component: DocentesComponent,
    },
];

export default docentesRoutes;
