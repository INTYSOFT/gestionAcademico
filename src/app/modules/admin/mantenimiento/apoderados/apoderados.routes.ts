import { Routes } from '@angular/router';
import { ApoderadosComponent } from './apoderados.component';

export const apoderadosRoutes: Routes = [
    {
        path: '',
        component: ApoderadosComponent,
    },
];
