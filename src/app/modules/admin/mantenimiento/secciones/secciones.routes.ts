import { Routes } from '@angular/router';
import { SeccionesComponent } from './secciones.component';

const seccionesRoutes: Routes = [
    {
        path: '',
        component: SeccionesComponent,
    },
];

export default seccionesRoutes;
